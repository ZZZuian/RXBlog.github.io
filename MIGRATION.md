# TinyDB → SQLite 迁移与回滚

阶段一启动时会自动创建 `community.db`，并将旧 `db.json` 中的账号、资料、帖子和评论迁移到 SQLite。迁移状态记录为 `tinydb_to_sqlite_v1`，重复启动不会重复导入。

## 自动备份

迁移器不会删除或修改 `db.json`，并会额外生成：

```text
db.json.pre-sqlite-backup
```

开始迁移前应停止正在运行的 Flask 进程，避免迁移期间仍有旧程序写入 JSON。

## 数据核对

迁移完成后至少核对：

- users 数量等于旧 auth 数量；
- posts 数量等于旧 entries 数量；
- 能关联到原帖的 comments 数量与旧评论数量一致；
- 最早的旧账号具有 admin 角色；
- 旧账号仍可使用原账号和密码登录。

如果是没有旧数据的全新安装，第一个成功注册的账号自动成为初始管理员，之后注册的账号默认为普通用户。

无法关联到帖子的历史评论不会强行导入，避免把评论挂到错误帖子；迁移日志和源 JSON 可用于人工核对。

## 回滚步骤

1. 停止 Flask 服务。
2. 保留 `community.db` 作为问题现场，不要覆盖它。
3. 将 `db.json.pre-sqlite-backup` 复制为单独的恢复文件。
4. 切回迁移前的代码版本后，再使用恢复文件启动旧版程序。

注意：切回 TinyDB 后，迁移完成之后写入 SQLite 的新帖子和互动不会自动出现在旧 JSON 中，因此正式启用社区版本前应先完成验收。
